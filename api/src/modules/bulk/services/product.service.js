import * as bulk from "../service.js";
export const list = bulk.listProducts;
export const getById = bulk.getProduct;
export const create = bulk.createProduct;
export const update = bulk.updateProduct;
export const remove = bulk.deleteProduct;
